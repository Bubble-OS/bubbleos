const { VERSION } = require("../variables/constants");
const { date } = require("../variables/variables");

console.log(`BubbleOS Lite, ${date} (v${VERSION})`);
console.log(`Copyright (c) ${date} Bubble, Inc. All rights reserved.`);

console.log();

console.log("For help on some available commands, type 'help'.");
console.log("For more information about a command, type 'help <command>'.");

console.log();

console.log("To exit the BubbleOS Lite shell, type 'exit'.");

console.log();
